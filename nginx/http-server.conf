server {
    listen 127.0.0.1:802;
    root /var/www/http-server;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /health {
        add_header Content-Type text/plain;
        return 200 "ok\n";
    }
}